import React from 'react';

const Header = () => {
  return (
    <header className="text-center py-4 mb-3">
      <h1 className="app-title">AI Text Organizer</h1>
      <p className="text-muted">
        Analyzuje, organizuje a formátuje text s automatickým číslováním a nadpisy s datem
      </p>
    </header>
  );
};

export default Header;
